import React from "react";
import Button from "grommet/components/Button";

const ContributeButton = ({ onClick }) => {
  return <Button onClick={onClick} label="Contribute" />;
};

export default ContributeButton;
